============================== top module ==============================
NOTEs::
    1. This top moudle is general and scalable
    2. The core can be used for any number of channels by configuration of "num_ch" parameter
    3. The default number of channels is 4, "num_ch = 4"
    4. The interface is wb_slave complaint B4
    5. TB of top moudle has counters for all test cases, PASSed , and FAILed ones
Verification Scenarios::
    # Functional Correctness Scenarios
        1. RESET behavior                                                           # PASSed
        2. Direct wb write/read for all registers                                   # PASSed
        3. Random wb write/read for all registers                                   # PASSed
        4. pwm core: DC_reg = 50 percent, period_reg = 4                            # PASSed
        5. pwm core: DC_reg = 25 percent, period_reg = 8                            # PASSed
        6. pwm core: i_DC = 75 percent, period_reg = 4                              # PASSed
        7. pwm core: random DC = 40 percent, period_reg = 5                         # PASSed
        8. timer core: [one-shot mode] period_reg = 4                               # PASSed
        9. timer core: [cont mode] period_reg = 4                                   # PASSed
        10. timer core: [random mode] period_reg = 7                                # PASSed
        11. Random core: DC = 50 percent, period_reg = 4                            # PASSed
        12. timer core: [random mode] period_reg = 4, divisor_reg = 4               # PASSed
    # Corner Case Scenarios
        1. pwm core: [DC_reg = 4] > [period_reg = 3]                                # PASSed
        2. pwm core: [i_DC = 5] > [period_reg = 4]                                  # PASSed
        3. timer core: [random mode] period_reg = 1                                 # PASSed
        4. assign different data for each channel and cheack outputs in parallel    # PASSed
TB TASKs::
    1. reset()
    2. assign_data()
    3. assert_pwm()
    4. assert_timer()
    5. assert_interface()